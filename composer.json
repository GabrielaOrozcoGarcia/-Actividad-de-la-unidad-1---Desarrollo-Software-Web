{
    "name": "crud-usuarios/app",
    "description": "CRUD de usuarios con arquitectura hexagonal",
    "type": "project",
    "require": {
        "php": ">=8.1"
    },
    "autoload": {
        "classmap": [
            "src/"
        ]
    },
    "config": {
        "optimize-autoloader": true
    }
}